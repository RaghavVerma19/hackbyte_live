import DocsClient from "./DocsClient";

export const metadata = {
  title: "VerifAI API Docs — Resume Fraud Detection as a Service",
  description: "Integrate VerifAI's resume verification API into your recruiting software. Detect inflated skills, fake coding profiles, and GitHub fraud in seconds.",
};

export default function DocsPage() {
  return <DocsClient />;
}
